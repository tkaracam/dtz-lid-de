const API_BASE = window.location.hostname === 'localhost' 
    ? 'http://localhost:8080/api' 
    : '/api';
const FRONTEND_BASE = window.location.hostname === 'localhost'
    ? 'http://localhost:8080/frontend'
    : '/frontend';
